---
import { services } from '../../data/services';
---

<section class="section bg-[rgb(var(--color-surface))]">
	<div class="site-container">
		<div class="flex items-end justify-between gap-6">
			<div>
				<h2 class="text-2xl font-semibold tracking-tight">Leistungen</h2>
				<p class="mt-2 max-w-2xl text-sm leading-6 text-[rgb(var(--color-muted))]">
					Unsere Leistungen sind auf die zentralen Qualitätsfragen moderner Gebäude ausgerichtet: bauphysikalische Sicherheit, energetische Effizienz und technische Konsistenz zwischen Planung und Ausführung. 
				</p>
			</div>
			<a class="hidden text-sm text-[rgb(var(--color-primary))] no-underline hover:opacity-80 md:block" href="/leistungen">
				Alle Leistungen
			</a>
		</div>

		<div class="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">
			{services.slice(0, 6).map((service) => (
				<a
					class="card block p-6 no-underline hover:border-black/20"
					href={`/leistungen/${service.slug}`}
				>
					<div class="flex items-start gap-4">
						<div
							class="flex h-10 w-10 items-center justify-center rounded-xl bg-black/5 text-[rgb(var(--color-primary))]"
							aria-hidden="true"
						>
							<span class="text-sm font-semibold">{service.title.slice(0, 1)}</span>
						</div>
						<div class="min-w-0">
							<h3 class="text-base font-semibold">{service.title}</h3>
							<p class="mt-2 text-sm leading-6 text-[rgb(var(--color-muted))]">
								{service.shortDescription}
							</p>
							<span class="mt-4 inline-flex text-sm font-medium text-[rgb(var(--color-primary))]">
								Details ansehen →
							</span>
						</div>
					</div>
				</a>
			))}
		</div>
	</div>
</section>


